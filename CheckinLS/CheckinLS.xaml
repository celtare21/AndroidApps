<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CheckinLS.CheckinLS"
             FlyoutBackgroundColor="Black">

    <Shell.ItemTemplate>
        <DataTemplate>
            <StackLayout Padding="10,20,10,20" Style="{StaticResource FloutItemStyle}">
                <Label Text="{Binding Title}" TextColor="AntiqueWhite" VerticalOptions="Center" 
                   HorizontalOptions="Center" FontSize="20" FontAttributes="Bold" FontFamily="sans-serif"/>
            </StackLayout>
        </DataTemplate>
    </Shell.ItemTemplate>
</Shell>